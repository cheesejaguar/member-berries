# Checkpoint

- Time: 2026-04-10T00:00:00Z
- Trigger: manual
- Session: investigate search indexing delay

## Objective
Understand why newly created notes sometimes do not become searchable immediately.

## Completed
- Traced the intended flow from note creation to embedding worker to search index update.
- Confirmed that manual reindex restores searchability for affected notes.

## Current State
The system likely has a worker retry or idempotency issue rather than a primary API write problem.

## Important Findings
- Reindex can repair state without proving the original async path is healthy.
- Queue retry behavior needs more visible instrumentation.

## Open Questions
- Why does the first worker attempt fail?
- Are duplicate events contributing to inconsistent search results?

## Next Steps
- Add instrumentation around first worker failure and retry count.
- Reproduce with a single note and inspect queue logs.

## Touched Areas
- `src/jobs/embedding-worker.ts`
- `src/core/search-index.ts`
- `scripts/run-worker.ts`

## Commands Run
- `pnpm worker:dev`
- `pnpm reindex:note -- --id 42`

## Notable Errors / Surprises
- Manual reindex makes the system look healthy even when the original queue path is still unreliable.
